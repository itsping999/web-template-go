apiVersion: v1
kind: Service
metadata:
  name: web-template-go
  labels:
    app.kubernetes.io/name: web-template-go
    app.kubernetes.io/component: api
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: web-template-go
    app.kubernetes.io/component: api
  ports:
    - name: http
      port: 8000
      targetPort: http
    - name: grpc
      port: 9000
      targetPort: grpc
